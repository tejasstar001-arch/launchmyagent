# Launch My Agent Website (Concrete Build)

This is a deployable static website starter.

## Files
- index.html (homepage)
- products.html (product catalog)
- styles.css (styling)

## Next step
Replace placeholder Stripe `href="#"` links in `products.html` with live payment links.

## Local preview
Open `index.html` directly in browser or serve this folder with any static server.
